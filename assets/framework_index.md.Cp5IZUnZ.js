import{_ as s,o as a,c as i,a4 as o}from"./chunks/framework.LQ2Ga-wM.js";const h=JSON.parse('{"title":"Introduction à lo","description":"","frontmatter":{},"headers":[],"relativePath":"framework/index.md","filePath":"framework/index.md","lastUpdated":1781601194000}'),n={name:"framework/index.md"};function t(r,e,l,d,c,p){return a(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="introduction-a-lo" tabindex="-1">Introduction à lo <a class="header-anchor" href="#introduction-a-lo" aria-label="Permalink to &quot;Introduction à lo&quot;">​</a></h1><p><code>lo</code> est le framework RedM monolithique de Last Country. C&#39;est une <strong>ressource unique</strong> qui agrège le noyau (sessions, comptes, joueurs, personnages) et l&#39;ensemble de ses systèmes sous un seul objet global : <code>Core</code>. Conçu et testé exclusivement pour <strong>RDR3 / RedM</strong>, il ne vise jamais FiveM. Le code est organisé en <strong>domaines plats</strong>, chaque gros système vit dans son propre dossier de domaine, et toute primitive de bas niveau passe par un wrapper <code>Core.*</code> cohérent.</p><p>Cette page présente l&#39;identité du framework (version 2.0.0, refonte <strong>classes/services</strong>), la carte de son dépôt, le principe de structure par domaine, sa fondation <code>jo_libs</code> et la règle des wrappers obligatoires. Les pages suivantes détaillent l&#39;installation, l&#39;architecture, la configuration et l&#39;API.</p><h2 id="ce-qu-est-lo" tabindex="-1">Ce qu&#39;est lo <a class="header-anchor" href="#ce-qu-est-lo" aria-label="Permalink to &quot;Ce qu&#39;est lo&quot;">​</a></h2><div class="info custom-block"><p class="custom-block-title">Ressource unique, objet global <code>Core</code></p><p>Tout le framework tient dans une seule ressource nommée <code>lo</code>. Tous les domaines s&#39;exposent sous le même objet global <code>Core</code>. Il n&#39;existe pas de noyau séparé à démarrer à côté.</p></div><p>Caractéristiques structurantes :</p><ul><li><strong>Monolithique</strong> : un seul <code>fxmanifest.lua</code>, un seul point de démarrage (<code>init.lua</code>), une seule arborescence de configuration (<code>config/*.lua</code>). Chaque domaine s&#39;accroche au global <code>Core</code> ; le couplage inter-domaine passe par des events <code>lo:&lt;côté&gt;:&lt;domaine&gt;:&lt;action&gt;</code> et par les hooks de cycle de vie.</li><li><strong>Cible RDR3 uniquement</strong> : le manifeste déclare <code>game &#39;rdr3&#39;</code>. Le code utilise des natives et des comportements propres à RedM.</li><li><strong>Serveur-autoritaire</strong> : le cœur du framework est la <strong>Session</strong>, une machine à états par joueur possédée par le serveur. Le client ne décide jamais de son propre état ; il exécute les phases que le serveur lui ordonne.</li><li><strong>Architecture classes/services</strong> : les systèmes sont des classes (<code>shared/class.lua</code>, helper <code>class()</code>), instanciées et accrochées au global <code>Core</code> (par exemple <code>Core.Lottery</code>, <code>Core.Vip</code>, <code>Core.Inventory</code>, <code>Core.Player</code>).</li></ul><p>Le manifeste déclare l&#39;identité et les capacités fournies de la ressource :</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">fx_version</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;cerulean&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">game</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;rdr3&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;lo&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">version</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;2.0.0&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">provide</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;rsg-core&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">provide</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;rsg-inventory&#39;</span></span></code></pre></div><p><code>lo</code> <strong>remplace</strong> RSGCore et son inventaire : il les fournit (<code>provide</code>) aux ressources tierces, qui voient <code>RSGCore</code> / <code>RSGInventory</code> via les bridges (<code>bridge/</code>). Il ne faut donc jamais démarrer <code>rsg-core</code> en parallèle.</p><div class="warning custom-block"><p class="custom-block-title">Architecture v2 — ce qui n&#39;existe plus</p><p>La refonte classes/services a supprimé plusieurs systèmes de l&#39;ancienne architecture (v1) : <strong>pas</strong> de registre de modules (<code>Core.RegisterModule</code>), <strong>pas</strong> d&#39;éditeur in-game (<code>/editor</code>, <code>editor_overrides</code>), <strong>pas</strong> de <code>Core.World.Add</code> / <code>Core.Action.register</code>, <strong>pas</strong> de layout <code>core/</code> + <code>modules/</code>. La configuration staff passe désormais par la ressource externe <strong><code>lo_jobscreator</code></strong> (panels <code>Core.PanelHost</code>), et non par un éditeur intégré.</p></div><h2 id="carte-du-depot" tabindex="-1">Carte du dépôt <a class="header-anchor" href="#carte-du-depot" aria-label="Permalink to &quot;Carte du dépôt&quot;">​</a></h2><p>La structure du dépôt est <strong>plate par domaine</strong> : chaque dossier de premier niveau est un domaine, avec ses sous-dossiers <code>server/</code>, <code>client/</code>, <code>shared/</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>resources/[core]/lo/</span></span>
<span class="line"><span>├── fxmanifest.lua          source de vérité de l&#39;ordre de chargement (pas de globs auto)</span></span>
<span class="line"><span>├── init.lua                naissance de Core + chargement NUI (jo.nui.load vers lo_uiv2)</span></span>
<span class="line"><span>├── shared/                 class.lua, logger, utils, enums, event, callback, command,</span></span>
<span class="line"><span>│                           scheduler, lifecycle, jobs, money, trace</span></span>
<span class="line"><span>├── config/                 config.lua + une feuille par domaine (défauts de la table Config)</span></span>
<span class="line"><span>├── account/                {server} comptes (fivem:), bans</span></span>
<span class="line"><span>├── session/                {server,client} machine à états, file d&#39;attente, spawn, boot</span></span>
<span class="line"><span>├── player/                 {server} classe LoPlayer + service Core.Player + save</span></span>
<span class="line"><span>├── character/              {server,client} CRUD personnages, créateur, skin/preview/select</span></span>
<span class="line"><span>├── inventory/              {server,client,shared} Core.Inventory : items, shops, craft, drops</span></span>
<span class="line"><span>├── items/                  {server} cache items single-source (table items en DB)</span></span>
<span class="line"><span>├── jobs/                   {server} métiers et sociétés</span></span>
<span class="line"><span>├── vip/  conditions/       {server} tiers VIP (Discord) et grants (slots/poids)</span></span>
<span class="line"><span>├── permissions/            {server} niveaux staff</span></span>
<span class="line"><span>├── panels/                 {server} panels staff exposés à lo_jobscreator</span></span>
<span class="line"><span>├── bridge/                 {server,client} compat RSG + dispatchers panelhost/typeconfig</span></span>
<span class="line"><span>├── world/                  {server,client} primitives Core.Ped/Prop/Blip/Marker/Interact…</span></span>
<span class="line"><span>├── hud/  voice/  notify/   {server,client} besoins, voix de proximité, notifications</span></span>
<span class="line"><span>├── net/  ui/  input/       réseau, menus/prompts/input, claviers</span></span>
<span class="line"><span>├── admin/  anticheat/      outils staff et couches de protection</span></span>
<span class="line"><span>├── death/ jail/ doors/     systèmes de jeu (mort, prison, portes…)</span></span>
<span class="line"><span>├── business/ police/       commerces, métier police</span></span>
<span class="line"><span>├── lockpicking/ gift/      crochetage, dons</span></span>
<span class="line"><span>├── vipstore/ lottery/      boutique VIP, loterie</span></span>
<span class="line"><span>├── emotes/ imaps/ pause/   émotes, objets de carte, menu pause</span></span>
<span class="line"><span>├── spawn/                  point d&#39;entrée unique de spawn (client)</span></span>
<span class="line"><span>└── activities/             {bee, foliage, hunting} activités de récolte/chasse</span></span></code></pre></div><p>Le front React n&#39;est pas dans cette ressource : il vit dans la ressource séparée <strong><code>lo_uiv2</code></strong> (<code>resources/[ui]/lo_uiv2/web/</code>), et <code>lo</code> le charge via le NUI de <code>jo_libs</code> (<code>jo.nui.load(&#39;lo&#39;, &#39;nui://lo_uiv2/web/build/index.html&#39;)</code>).</p><div class="tip custom-block"><p class="custom-block-title">Ordre de chargement explicite</p><p>Le <code>fxmanifest.lua</code> liste chaque fichier dans l&#39;ordre voulu — <strong>aucun glob auto, aucun préfixe <code>aaa_</code>/<code>zzz_</code></strong>. Un fichier qui étend une table doit être listé après celui qui la crée (par exemple <code>items/server/items.lua</code> avant les domaines qui déclarent des items, <code>bridge/server/panelhost.lua</code> avant les <code>panels/server/*</code>).</p></div><h2 id="structure-par-domaine-—-ou-va-le-code" tabindex="-1">Structure par domaine — où va le code <a class="header-anchor" href="#structure-par-domaine-—-ou-va-le-code" aria-label="Permalink to &quot;Structure par domaine — où va le code&quot;">​</a></h2><p>La structure par domaine est la colonne vertébrale du framework.</p><ul><li><strong>Tout gros système est un domaine</strong> : il vit dans <code>&lt;domaine&gt;/{server,client,shared}</code> et expose son service sur <code>Core</code>. Il n&#39;y a ni dossier <code>core/</code> ni dossier <code>modules/</code>, ni registre central : ajouter un domaine = créer le dossier et l&#39;ajouter au manifeste dans le bon ordre.</li><li><strong>Les services sont des classes</strong> : <code>class()</code> (de <code>shared/class.lua</code>) crée la classe, qu&#39;on instancie et accroche au global, par exemple <code>Core.Lottery = LotteryService()</code>.</li><li><strong>Le couplage inter-domaine</strong> passe par les events <code>lo:Server:&lt;domaine&gt;:&lt;action&gt;</code> / <code>lo:Client:&lt;domaine&gt;:&lt;action&gt;</code> et par les hooks de cycle de vie (<code>Core.Ready</code>, <code>Core.OnPlayerLoaded</code>, <code>Core.OnPlayerUnloaded</code>, <code>Core.OnPlayerSave</code>) — jamais par des globales croisées non documentées.</li></ul><div class="warning custom-block"><p class="custom-block-title">Aucune liste en dur dans la logique</p><p>Les données statiques (libellés, catalogues, hashes, teintes) ne vivent jamais au milieu d&#39;un fichier de logique. Elles ont des emplacements canoniques : <code>&lt;domaine&gt;/shared/data/</code> (partagé), <code>&lt;domaine&gt;/client/data/</code> (client), ou <code>config/</code> pour les réglages numériques.</p></div><h2 id="la-fondation-jo-libs" tabindex="-1">La fondation : <code>jo_libs</code> <a class="header-anchor" href="#la-fondation-jo-libs" aria-label="Permalink to &quot;La fondation : \`jo_libs\`&quot;">​</a></h2><p><code>lo</code> repose sur la bibliothèque <code>jo_libs</code> comme <strong>fondation obligatoire</strong>, sans fallback ni système parallèle. Le manifeste charge la lib et déclare les modules dont le framework a besoin :</p><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">shared_script</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;@jo_libs/init.lua&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;shared/class.lua&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;init.lua&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    -- config/*, shared/*, ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;player&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;database&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;component&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;ped-texture&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;menu&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;input&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;prompt-nui&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;callback&#39;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">jo_lib</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;nui&#39;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- ...</span></span></code></pre></div><p>Toute primitive de bas niveau passe par <code>jo_libs</code> : <code>jo.callback</code>, <code>jo.database</code>, <code>jo.menu</code>, <code>jo.input</code>, <code>jo.promptNui</code>, <code>jo.component</code>, <code>jo.pedTexture</code>, <code>jo.player</code>, <code>jo.nui</code>. La règle pratique : si <code>jo_libs</code> le fait, on l&#39;utilise ; s&#39;il le fait à moitié, on construit autour ; sinon seulement on implémente, dans le style de la lib.</p><div class="tip custom-block"><p class="custom-block-title">Vérifier l&#39;API réelle dans le code de la lib</p><p>La documentation en ligne de <code>jo_libs</code> est parfois inexacte, et plusieurs modules ont un nom de dossier différent de leur usage (<code>ped-texture</code>, <code>prompt-nui</code>). Toujours vérifier l&#39;API réelle dans <code>[lib]/jo_libs/modules/&lt;module&gt;/</code>.</p></div><div class="danger custom-block"><p class="custom-block-title">Jamais <code>jo.framework</code> / <code>jo.fw</code> dans le code de lo</p><p>Tout passe par NOTRE noyau et NOTRE inventaire (<code>Core.Player:Get(src)</code>, <code>Core.Inventory.*</code>, statebags <code>userId</code>/<code>charId</code>). Le framework-bridge de <code>jo_libs</code> détecte <code>rsg-core</code>, qui est notre propre <code>provide</code> : l&#39;utiliser depuis <code>lo</code> est un aller-retour absurde qui casse le démarrage. Les bridges ne servent qu&#39;aux ressources externes.</p></div><h2 id="le-principe-des-wrappers-core" tabindex="-1">Le principe des wrappers <code>Core.*</code> <a class="header-anchor" href="#le-principe-des-wrappers-core" aria-label="Permalink to &quot;Le principe des wrappers \`Core.*\`&quot;">​</a></h2><p>C&#39;est l&#39;un des principes les plus stricts du framework : <strong>toute primitive <code>jo_libs</code> et toute primitive monde possède un wrapper <code>Core.*</code>, et le code n&#39;appelle jamais le <code>jo.*</code> brut ni la native brute</strong> — uniquement le wrapper. C&#39;est ce qui rend le framework cohérent, débogable et réutilisable d&#39;un domaine à l&#39;autre.</p><table tabindex="0"><thead><tr><th>À utiliser</th><th>Jamais</th></tr></thead><tbody><tr><td><code>Core.Notify(msg, kind, dur)</code> / <code>Core.Notify(src, msg, kind, dur)</code> (serveur)</td><td><code>jo.notif.*</code></td></tr><tr><td><code>Core.Callback.register</code> / <code>.triggerServer</code> / <code>.triggerClient</code></td><td><code>jo.callback.*</code></td></tr><tr><td><code>Core.Menu.*</code></td><td><code>jo.menu.*</code></td></tr><tr><td><code>Core.Prompt.createNative</code> / <code>.createTarget</code> / <code>.createFollow</code> / <code>.createSimple</code></td><td><code>jo.prompt.*</code></td></tr><tr><td><code>Core.Input.text</code> / <code>.number</code> / <code>.select</code> / <code>.confirm</code> / <code>.form</code></td><td><code>jo.input.*</code></td></tr><tr><td><code>Core.RegisterKey(...)</code> / <code>Core.Keys.*</code></td><td><code>RegisterKeyMapping</code></td></tr><tr><td><code>Core.RegisterCommand(name, fn, { level, suggestion })</code></td><td><code>RegisterCommand</code></td></tr><tr><td><code>Core.Ped</code> / <code>Prop</code> / <code>Vehicle</code> / <code>Horse</code> / <code>Blip</code> / <code>Marker</code> / <code>Text3D</code> / <code>Interact</code></td><td><code>CreatePed</code> / <code>CreateObject</code>… bruts</td></tr><tr><td><code>Core.SafeTriggerClientEvent(name, target, ...)</code></td><td><code>TriggerClientEvent(..., -1, ...)</code> direct</td></tr><tr><td><code>Core.Scheduler.Register(name, interval, fn)</code></td><td>boucle <code>while true do Wait()</code> ad hoc</td></tr></tbody></table><p>Les wrappers <code>Core.Callback</code> et <code>Core.Menu</code> sont des proxies transparents (<code>setmetatable({}, { __index = jo.callback / jo.menu })</code>) : l&#39;API sous-jacente est identique, mais l&#39;indirection est obligatoire.</p><p>Conséquences à retenir :</p><ul><li><strong>Le serveur ne crée jamais d&#39;entité</strong> : seul le client spawn, via les primitives <code>Core.Ped</code> / <code>Core.Prop</code> / <code>Core.Vehicle</code> (qui gèrent streaming, nettoyage et cohérence). Jamais de native brute dans la logique.</li><li><strong>Ne jamais redéclarer des touches en local</strong> : utiliser <code>Core.RegisterKey(name, desc, defaultKey)</code> / <code>Core.Keys.*</code>, jamais <code>RegisterKeyMapping</code>.</li><li><strong>Tout broadcast <code>-1</code></strong> passe par <code>Core.SafeTriggerClientEvent</code> (latent si &gt; ~15 Ko, sinon kick 16 Ko de tout le serveur).</li></ul><div class="language-lua vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Core.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RegisterKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;myaction&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Mon action&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;F5&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">onPressed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Core.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Notify</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Action déclenchée&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;success&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="pour-aller-plus-loin" tabindex="-1">Pour aller plus loin <a class="header-anchor" href="#pour-aller-plus-loin" aria-label="Permalink to &quot;Pour aller plus loin&quot;">​</a></h2><ul><li><a href="./installation">Installation</a> — prérequis (<code>oxmysql</code>, <code>onesync</code>, <code>jo_libs</code>), ordre de démarrage dans <code>server.cfg</code>.</li><li><a href="./architecture">Architecture</a> — la Session serveur-autoritaire, le cycle de vie, l&#39;objet <code>LoPlayer</code>, les services et la base de données.</li><li><a href="./configuration">Configuration</a> — la table <code>Config</code> (<code>config/*.lua</code>), <code>Core.ConfigStore</code> et les panels staff <code>lo_jobscreator</code>.</li><li><a href="./modules/">Domaines</a> — la liste des domaines livrés et comment en ajouter un.</li><li><a href="./../api/core">API Core</a> — référence des wrappers <code>Core.*</code>, des events <code>lo:*</code> et des helpers de cycle de vie.</li></ul>`,35)])])}const g=s(n,[["render",t]]);export{h as __pageData,g as default};
